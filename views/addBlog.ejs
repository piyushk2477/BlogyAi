<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Add New Blog</title>

    <!-- Pixel Font -->
    <link href="https://fonts.googleapis.com" rel="preconnect"/>
    <link crossorigin href="https://fonts.gstatic.com" rel="preconnect"/>
    <link href="https://fonts.googleapis.com/css2?family=VT323&display=swap" rel="stylesheet"/>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-white font-[VT323]">

    <%- include('./partials/nav') %>

    <div class="max-w-3xl mx-auto mt-12 p-6 border-4 border-black shadow-[6px_6px_0px_black] bg-yellow-50 rounded-none">
      <h1 class="text-3xl font-bold mb-6 uppercase border-2 border-black px-2 py-1 shadow-[4px_4px_0px_black]">Add New Blog</h1>

      <form action="/blog" method="post" enctype="multipart/form-data" class="flex flex-col gap-6">

        <!-- Cover Image -->
        <div class="flex flex-col">
          <label for="coverImage" class="mb-2 font-bold uppercase border-b-2 border-black">Cover Image</label>
          <input type="file" id="coverImage" name="coverImage" 
                 class="border-2 border-black p-2 shadow-[4px_4px_0px_black] focus:outline-none"/>
        </div>

        <!-- Title -->
        <div class="flex flex-col">
          <label for="title" class="mb-2 font-bold uppercase border-b-2 border-black">Title</label>
          <input type="text" id="title" name="title" 
                 class="border-2 border-black p-2 shadow-[4px_4px_0px_black] focus:outline-none"/>
        </div>

        <!-- Body -->
        <div class="flex flex-col">
          <label for="body" class="mb-2 font-bold uppercase border-b-2 border-black">Body</label>
          <textarea id="body" name="body" rows="10" 
                    class="border-2 border-black p-2 shadow-[4px_4px_0px_black] focus:outline-none"></textarea>
        </div>

        <!-- Submit Button -->
        <div>
          <button type="submit" 
                  class="bg-white border-4 border-black shadow-[6px_6px_0px_black] 
                         hover:translate-x-1 hover:translate-y-1 transition-all px-6 py-2 font-bold uppercase">
            Submit
          </button>
        </div>

      </form>
    </div>

    <%- include('./partials/scripts') %>
  </body>
</html>
